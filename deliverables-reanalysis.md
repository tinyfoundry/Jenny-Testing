# DCF Reanalysis Deliverables

1. Final validated 6-domain list and explanations are in `data/content-map.json` (`domains` + `domain_validation`).
2. Full content map JSON: `data/content-map.json`.
3. Large-scale question bank JSON: `data/question-bank.json` (252+ questions including integrated subset).
4. Randomization engine pseudocode and schema: `data/randomization-engine.json`.
5. Expansion notes are documented in `data/randomization-engine.json` rules and scoring fields.
